# AutoResearch generated config.
# Hyperparameter change: slightly higher learning rate to test faster convergence without changing class balance
# Constraint: only one small hyperparameter change from the selected best recent config.
dataset:
  patch_size: 64
  train_npz: data/real_cross/segment_20230827161847/train.npz
  val_npz: data/real_cross/segment_20230520175435/val.npz
  validation_mode: cross-segment
evaluation:
  main_metric: val_f1
  threshold: 0.5
model:
  depth: 3
  name: tiny_numpy_ink_logreg
outputs:
  runs_dir: experiments/runs
training:
  epochs: 5
  learning_rate: 0.144
  pos_weight: auto
  weight_decay: 0.0001
validation_setup:
  mode: cross-segment
  train_scroll_id: '1'
  train_segment_id: '20230827161847'
  val_scroll_id: '1'
  val_segment_id: '20230520175435'
  warning: null
autoresearch:
  parent_reason: slightly higher learning rate to test faster convergence without
    changing class balance
