# AutoResearch generated config.
# Hyperparameter change: simpler feature depth to test whether cross-segment generalization improves with fewer texture terms
# Constraint: only one small hyperparameter change from the selected best recent config.
autoresearch:
  parent_reason: simpler feature depth to test whether cross-segment generalization
    improves with fewer texture terms
  scope_policy: focused_pair_only
  search_signature:
  - tiny_numpy_mlp
  - 2
  - 24
  - 2
  - 0.1152
  - 0.1518519
  - 8.0
  - 600000
  - null
  - 1337
dataset:
  research_scope: focused_pair
  scope_note: One train segment and one held-out validation segment; add folds only
    through explicit configs.
  train_npz: data/real_cross/segment_20230827161847/train.npz
  val_npz: data/real_cross/segment_20230520175435/val.npz
  patch_size: 64
  validation_mode: cross-segment
evaluation:
  main_metric: val_f1
  threshold: 0.031
model:
  depth: 2
  name: tiny_numpy_mlp
outputs:
  runs_dir: experiments/runs
training:
  epochs: 2
  learning_rate: 0.1152
  pos_weight: 8.0
  weight_decay: 0.1518519
